包装类
new Number(123); 叫做数字对象 
可以和对象一样拥有属性和方法，但是经过数字运算之后又变回了原始值数字

???原始值都有啥！！！！
原始值： number Boolean String undefined（未定义，未赋值） null（占位置，空值）

var num = new Number(123)
var str = new String("abcd")
var bol = new Boolean()
undefined 和 null 不能拥有属性

在原始值直接进行属性的赋值时。会有一个隐式的转换过程(包装类)。让原始值能够进行属性的赋值。
虽然不会报错，但是系统会删除赋值语句(全当这个语句没有存在过，不会对数据产生影响)。
等你下次使用这个值的是属性时，会再次生成一个包装类，但是没有赋值过程。所以会返回undefined；


.length是字符串拥有的系统自带属性，只不过是对象字符串才带有的属性。用str.length时会有一个隐式的(包装类)的过程。
！！！new String(str).length，让我们错以为str拥有.length属性



