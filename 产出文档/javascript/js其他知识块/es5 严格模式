"use strict";严格模式启动 
可以全局启动也可以函数内部局部启动

witch(obj){
    改变作用域链的最顶端(将obj作为此代码块的作用域最顶端，通常配合命名空间使用)
}  //效率会变低很多  es5严格模式禁用

arguments.callee()
function.caller()      //这两个es5严格模式禁用

变量赋值前必须声明  

局部的this必须被赋值  在es5的严格模式下，预编译下的this不会指向windows。但是全局的this还是指向window


拒绝重复的属性和参数

eval()可以把字符串当做代码执行

