双飞翼布局：
    技术要点：
        先定义middle，先加载主要内容区
        使用负margin使浮动元素上浮到上一列，实现三列布局
        middle中嵌套一个div，div设置margin，防止主要内容区被遮挡
        ？？？为什么不能直接在main上设置margin
        1.main的宽度是百分之百的情况下。额外加入margin会把布局撑开。两额外列会看不到。
        2.为了不撑开布局，把main的宽度设置为（100%-想要有的margin宽度）。(我自己做的，目前还没
        发现缺点，待商议)
    注意要点：
        三个列都要浮动才能使left和right上浮
        文档流中middle是第一列

  
